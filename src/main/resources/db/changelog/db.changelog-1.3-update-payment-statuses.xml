<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.15.xsd">

    <changeSet id="1.3.0" author="system">
        <comment>Update payment statuses that are not PAID or UNPAID to UNPAID</comment>
        
        <sql>
            UPDATE payments 
            SET status = 'UNPAID' 
            WHERE status NOT IN ('PAID', 'UNPAID')
        </sql>
        
        <rollback>
            -- Note: This rollback cannot restore original statuses as they were overwritten
            -- Consider this operation irreversible for status changes
            SELECT 'Payment status update cannot be rolled back - original statuses were overwritten' as warning;
        </rollback>
        
    </changeSet>

</databaseChangeLog> 